#include <filesystem>
#include "P2D/P2D.hpp"
#include "TS2/TS2.hpp"

int main(int, char **)
{
    // std::vector<P2D::Scenarios> expt = P2D::getExperiment(DIAG_BLOCK ? "TS2B" : "TS2N");

    // for (P2D::Scenarios &scens : expt)
    // {
    //     P2D::Grid grid;
    //     P2D::getMap(grid, scens);
    //     P2D::TS2::TS2<DIAG_BLOCK> TS2(&grid);

    //     P2D::run(&TS2, scens, SCEN_NUM);

    //     if (SCEN_NUM == 0)
    //         P2D::writeResults(scens);
    // }
    return 0;
}